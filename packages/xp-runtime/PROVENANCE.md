# Provenance

Original repository implementation for Issue #36. The width thresholds and interaction-lock contract follow the Issue's device-class doctrine. The previous primitive provider is replaced with one shared runtime context so AdaptiveOverlay and SnapRail continue participating in the same locks as application shells.
